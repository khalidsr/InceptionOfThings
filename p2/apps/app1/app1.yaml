apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-one
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-one
  template:
    metadata:
      labels:
        app: app-one
    spec:
      containers:
      - name: apache
        image: httpd:latest
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: apache-content
          mountPath: /usr/local/apache2/htdocs/
      volumes:
      - name: apache-content
        configMap:
          name: configmap_app-one
---
apiVersion: v1
kind: Service
metadata:
  name: serviceAppOne
spec:
  selector:
    app: app-one 
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP

