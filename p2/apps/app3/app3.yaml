apiVersion: apps/v1
kind: Deployment
metadata:
  name: app_three
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app_three
  template:
    metadata:
      labels:
        app: app_three
    spec:
      containers:
      - name: apache
        image: httpd:latest
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: apache-content
          mountPath: /usr/local/apache2/htdocs/
      volumes:
      - name: apache-content
        configMap:
          name: configmap_app_three
---
apiVersion: v1
kind: Service
metadata:
  name: service_app_three
spec:
  selector:
    app: app_three
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP

